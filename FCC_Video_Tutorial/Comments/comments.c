#include <stdio.h>
#include <stdlib.h>

int main()
{
	/* Anything in between these two symbols is a comment */

	/* This is a comment
		that spans multiple lines and will be ignored by the compiler
		
		Isn't that cool?
	*/

	printf("Comments are fun!"); // This is a comment

	/*
	printf("This line will not be executed, unless the comment tags are removed!");
	*/

	// This also works for single line comments
}